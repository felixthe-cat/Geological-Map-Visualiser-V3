# Virtual environment
.venv/
venv/
ENV/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/

# Temporary data and generated output models
temp_*.csv
t_*.csv
test_sp.csv
test_ori.csv
geology_model.glb
geology_model_*.glb
geology_model_vtk.zip
geology_model_render.png
test_model_output.glb
test_model_output.zip
test_model_output.png
*.vtk
screenshots/

# IDE configurations
.vscode/
.idea/

.vercel
# Every .env variant, not just *.local — the session Stop hook runs `git add -A`,
# so an un-ignored .env holding the Google OAuth client secret would be committed
# and pushed to a public repo automatically. .env.example is the one exception.
.env
.env.*
!.env.example
web/hero_globe.png

# Local-only: large source data, scratch work, agent config
# (/data/ holds the 14 MB CSDI sqlite — source for web/data/*.csv exports, not committed)
/data/
scratch/
tmp_check.txt
.claude/
restructure_app.py
References from Datagov/
